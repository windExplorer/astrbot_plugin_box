# 更新日志

## v0.9.1

字体瘦身：8.1MB → 约 4.7MB。

增强点：

- 思源圆体从全量 TTF（14.1MB）替换为 **GBK 子集 WOFF2**（3.6MB）：字符集覆盖 GBK 全部 21791 个中文/符号（输入法可输入的中文全覆盖，含龘璐爨等生僻字）+ ASCII + 全角/标点，WOFF2/Brotli 二次压缩；PIL/FreeType 实测直接加载渲染，字形与原版一致。
- 唯一的取舍：GBK 之外的字符（扩展 B 区极生僻字、外文字母）会显示为方框——这些字符输入法打不出来，出现在昵称里的概率可忽略。
- 安装 Pillow 官方 wheel 的环境（含 AstrBot Docker）均内置 Brotli 支持，可正常加载 WOFF2。

## v0.9.0

成员数据面板用 Vue 3 + Naive UI 重写，并修复回填后的统计刷新问题。

新功能：

- 面板 UI 全面重做（Vue 3 + Naive UI，库文件随包本地分发，无 CDN 依赖）：左侧**可扩展菜单**（在 `menuOptions` 数组追加条目即可加新页面）、概览页统计卡片、**n-data-table 列表分页**、成员详情用 **n-drawer 抽屉**、深浅色主题跟随面板（`n-config-provider`）。
- 群列表与成员列表显示**头像**：群头像（`p.qlogo.cn/gh`）与成员 QQ 头像（`q1.qlogo.cn`），加载失败自动回退为群名/昵称首字。
- 概览页新增统计卡片：群数量、成员总数、群内信息已记录、入群时间已记录。

Bug Fixes：

- 回填后统计仍显示缺少：`ensure_join_many` 原来用 `INSERT OR IGNORE`，历史事件留下的 `join_time=''` 占位行永远不会被回填。改为 `ON CONFLICT DO UPDATE ... WHERE join_time = ''`（只回填空占位、不覆盖事件精确值）。
- 回填完成后抽屉与统计数据不自动刷新：现在任务结束自动刷新群统计与当前打开的成员抽屉；成员列表自带刷新按钮。
- 成员状态徽章语义修正：主状态以群内信息记录为准；接口本身不提供 join_time 的成员不再误报「缺入群时间」，改显示「接口未提供」。

## v0.8.1

修复 v0.8.0 无法加载：`init_backfill` 漏声明配置字段，`initialize()` 访问时抛 `AttributeError`，生命周期钩子的异常导致整个插件加载失败（原因：init_backfill）。

Bug Fixes：

- `PluginConfig` 补声明 `init_backfill` 字段（与 v0.7.3 的 `welcome_images` 同类疏漏）。
- 已系统性核对：`_conf_schema.json` 全部 22 个顶层键与嵌套子键均已在配置类中声明。
- `initialize()` 生命周期钩子全程 try/except 兜底——钩子内任何异常只降级对应功能（面板/回填不可用并记日志），不再阻断插件加载。

## v0.8.0

新增成员数据面板（WebUI）与启动自动回填。

新功能：

- **初始化自动回填**（`init_backfill`，默认开）：插件启动等适配器就绪后，遍历所有群的成员列表，批量入库群昵称/头衔/身份/等级（`member_info`）与入群时间（`member_times`，已有精确值不被覆盖），群与群之间停歇 1 秒降低接口压力；12 小时内不重复执行。之后退群/被踢卡的信息行不再依赖「该成员曾被查询过」。
- **成员数据面板**（`pages/member_panel/`）：AstrBot 后台插件拓展页打开，查看各群「已记录 / 缺记录」统计（进度条）、逐群或一键读取全部、按群查看成员明细（可筛「仅看缺记录」、分页浏览）。后端经 `context.register_web_api` 注册 5 条路由（`/astrbot_plugin_box` 前缀），前端走官方 Page 桥接（遵循 sandbox 约束，无 localStorage 依赖）。
- 面板读取任务支持取消，进行中重复触发会被拒绝；卸载插件时取消任务并关闭数据库。

其他：

- `store.py` 新增 `member_info` 批量写入、群级统计、`meta` 键值表（记录上次回填时间）。
- `build_zip.ps1` 打包清单新增 `pages` 目录。

## v0.7.4

修复退群/被踢卡信息行不显示；被踢/退群卡可显示入群排位。

Bug Fixes：

- 退群/被踢卡上约定好的信息行（加群时间、被踢/退群时间、在群时长、群等级、群头衔）可能整体缺失：部分适配器对已离群成员**不报错但返回空/残留数据**，旧逻辑用 `not member_info` 判断「已离群」，半空字典判真导致补行逻辑整段跳过。现改为事件卡**无条件重组**：先剔除接口残留的同类行，再按固定顺序（加群时间 → 被踢/退群时间 → 在群时长 → 群等级 → 群头衔 → 操作管理员）用「DB 精确值优先、接口数据兜底」填充。

新功能：

- 退群/被踢卡显示**入群排位**：人已不在成员列表中，无法直接排序，现用库内记录的入群时间与现有成员的 join_time 对比估算（「早于其入群的人数 + 1」），排位行正常显示在卡片首行。

注意：加群时间/在群时长/排位依赖本地数据库记录——v0.6.0 之前加入且从未被查询过的成员没有入库数据，这几行会自动省略（接口层面无数据来源）。

## v0.7.3

修复旧配置存档缺键导致的崩溃（v0.7.1 引入的 `welcome_images` 未在 `PluginConfig` 声明，入群事件触发即报 `AttributeError: welcome_images`）。

Bug Fixes：

- `PluginConfig` 补声明 `welcome_images` 字段。
- 新增 `ConfigNode._fill_schema_defaults()`：插件加载时读取 `_conf_schema.json`，把用户存档中**缺失的配置键按 schema 默认值补齐**（含嵌套 object：整键缺失先补空字典再填子键，`ai_analysis` / `autobox` 均覆盖），补齐后写回存档。此前从 v0.5.0 及更早版本直升的用户还会在 `ai_analysis` 上遇到同类崩溃，一并解决。

## v0.7.2

确认欢迎图片池支持 WebP，配置说明补齐。

增强点：

- 欢迎图片池确认支持 **WebP**（Pillow 内置解码，按文件内容识别格式、与 URL 后缀无关），PNG/JPG/BMP/GIF 均可用；动图取第一帧。配置提示已补充说明。

## v0.7.1

欢迎语与欢迎图片池嵌入卡片内。

新功能：

- **欢迎图片池**（`welcome_images`）：http/https 图片链接列表，入群卡内随机嵌入一张——按卡片宽度等比缩放（最高 640px）、圆角居中展示，下载失败或留空时自动只显示文字。
- 欢迎语从「卡片后追加 @文本消息」改为**嵌入入群卡片内**：卡头下方新增主题色「欢迎你 · Welcome」区块（薄荷绿），多行模板文本自动换行，AI 欢迎语仍按开关追加在文末。嵌入后不再单独发文本消息。

实现说明：欢迎内容构建移入 `BoxService`（`_build_welcome` / `_download_image`），通过 `BoxResult.welcome_text / welcome_image` 传递给渲染器；入群卡连同欢迎区一起进入缓存体系。

## v0.7.0

整合 astrbot_plugin_group_welcome 的核心能力（取其精华）。

新功能：

- **入群欢迎语**（`welcome_enabled`，默认开）：入群卡发出时在同一消息里 @ 新成员并追加欢迎语文本。模板支持 `{name}`（群昵称，回退昵称/QQ号）与 `{count_text}` 占位符，后者自动填充为「你是本群第 N 位成员（共 M 人）」——数据来自入群排位的 join_time 排序，比原插件的 member_count 更准确。
- **AI 欢迎语**（`welcome_ai_enabled`，默认关）：在欢迎语后追加一句 LLM 生成的个性化欢迎语，提示词可配置（`{name}` 占位），支持重试次数（`welcome_ai_retry`，默认 1），全部失败时静默跳过。
- **私聊群规**（`welcome_private_rules`，默认关）：新成员入群卡展示片刻后私聊发送群规文本（`group_rules`）。
- **群黑名单**（`black_groups`）：自动展示的排除名单；白名单为空时生效，白名单优先。

取舍说明：原插件的欢迎图片池不整合（我们的卡片本身就是图片）；欢迎冷却不整合（入群事件天然只触发一次，且卡片侧已有缓存冷却）。

其他：

- `BoxResult` 新增 `display_name` / `join_pos` / `join_total` 字段；`_get_join_rank` 改为返回 (文本, 位置, 总数)。
- 插件卸载时关闭 SQLite 连接。

## v0.6.3

按需求重整退群/被踢卡的信息行。

新功能：

- **成员群内信息缓存**：SQLite 新增 `member_info` 表，每次成功拉取群成员信息时保存最后已知的群昵称/群头衔/群身份/群等级（`put_member_info`，事件写入不会用空数据覆盖）。人离开后接口查不到这些字段，退群/被踢卡从缓存补显。
- 退群/被踢卡新增「群等级」「群头衔」两行（取最后已知值，从未被查询过则无此数据、自动省略）。
- 被踢卡的时间行标签由「退群时间」改为「**被踢时间**」，退群卡保持「退群时间」。

## v0.6.2

修复事件卡颜色不生效；新增退群/被踢卡 LLM 开关。

Bug Fixes：

- 入群/退群/被踢卡在线上始终是粉色：`render_box_image` 调用渲染器时**漏传了 `result.card_type`**，卡头/页脚/描边的类型配色从未生效（预览脚本直接给 `create()` 传参所以没暴露）。已补上该参数，并逐类型采样卡头像素验证配色正确。

增强点：

- 新增配置 `ai_analysis.event_analysis`（默认**关闭**）：退群卡和被踢卡默认不调用 LLM 生成分析（省 token，且此时两者分析块整体省略）；开启后按头像分析/签名解读/综合锐评三个开关执行。入群卡与普通查询卡行为不变。

## v0.6.1

卡片类型整体配色区分（原来仅靠小角标，太不显眼）。

增强点：

- 入群 / 退群 / 被踢三种事件卡的**卡头渐变、页脚、描边、角标**全部按类型换色：入群=薄荷绿、退群=紫灰、被踢=珊瑚红、普通查询=粉色不变。远看整体色调即可分辨卡片类型。
- 角标加大（字号 24 → 28）并改为**白底 + 主题色描边 + 主题色文字**，与换色后的卡头搭配清晰醒目；页脚文字/爱心/猫爪区同步使用主题色。
- 普通查询卡的粉色主题与原有视觉完全一致。

## v0.6.0

新增资料卡缓存冷却与全局限流队列。

新功能：

- **资料卡缓存**（`cache_cooldown`，默认 15 分钟，0 关闭）：每次实时获取的卡片（含图片、文本字段、AI 锐评）按「群 + 用户」存入 SQLite `card_cache` 表；同一群同一用户在冷却时间内再次获取时，直接返回库里的上一张卡片——不重新请求 QQ 接口、不消耗 LLM token、不排队。缓存行会随写入自动清理冷却窗口外的旧数据，表不会无限增长。冷却期内的卡片页脚仍是原获取时间（如实反映数据新鲜度）。
- **并发获取队列**（`max_concurrent`，默认 1）：全局信号量，所有群所有用户同时最多 N 个获取任务在请求接口/LLM，其余按顺序排队；排队期间若同一目标已被其他任务刷新则直接命中缓存。走缓存的请求完全绕过队列。

设计说明：

- 入群/退群/被踢事件卡**不受冷却限制**（退群卡必须反映当下），但会刷新缓存。
- 新增 `BoxService.fetch_card()` 统一入口（缓存查找 → 信号量 → 获取 → 渲染 → 写缓存），指令 / LLM 工具 / 事件三条路径全部收口；`send_box_image` 改为直接使用 `result.image`，不再重复渲染。

## v0.5.0

区分四种资料卡，并支持被踢事件。

新功能：

- **被踢资料卡**：原来只处理 `group_decrease` 的 `leave`（主动退群），管理员/群主踢人（`sub_type == "kick"`）不触发任何展示。现在踢人事件同样入库、同样按 `autobox.exit` 开关自动展示资料卡，并解析事件里的 `operator_id`，卡片上新增「操作管理员」行（尝试反查踢人者的昵称，失败则只显示 QQ 号）。
- **卡片类型角标**：卡头右上角按事件类型显示彩色胶囊角标——入群卡「新朋友」（绿）、退群卡「已退群」（紫灰）、被踢卡「被踢出群」（红）；普通查询卡不显示角标（保留星光装饰）。标题布局会为角标预留宽度，长昵称不会与之重叠。
- **退群/被踢卡专属行**：在「退群时间」之外新增「在群时长：N 天」（由库内精确的入群/退群时间相减得出）。退群/被踢卡上「加群时间」「退群时间」不受「加群时间」展示选项限制，强制显示（这是这两类卡的核心信息）；入群卡的「加群时间」不带「已入群 0 天」后缀。

## v0.4.0

扩充卡片可展示的信息字段。

新功能：

- **群身份**（`role`）：群主 / 管理员 / 成员，来自 `get_group_member_info`，紧跟「群头衔」之后。
- **最后发言**（`last_sent_time`）：`YYYY-MM-DD HH:MM`，来自 `get_group_member_info`，紧跟「加群时间」之后。
- **获赞**：QQ 名片获赞数，走 NapCat 扩展接口 `get_profile_like`（防御式解析多种返回结构）；适配器不支持或赞数为 0 时该行自动隐藏，不影响出卡。
- **入群天数**：附加在「加群时间」值后（`2024-05-01（已入群 871 天）`），数据库精确时间替换该行时同样保留天数后缀。

以上三项均加入 `_conf_schema.json` 的展示选项并默认开启，可在配置中勾掉。

## v0.3.3

修复无法查询 Bot 自己的资料卡。

Bug Fixes：

- `/资料卡 @bot` 之前展示的仍是发送者自己的卡片：上游代码在 `_get_ats` 里用 `ats.discard(event.get_self_id())` 把 Bot 自己从查询目标中强制剔除，目标为空后回落到发送者本人。现移除该剔除，指令可以正常查询 Bot 的资料卡。
- 同步放开 LLM 工具（`llm_box_user`）对「查询 Bot 自己」的拦截，行为与指令保持一致。Bot 自身的入群/退群事件仍不触发自动展示卡片（`handle_group_add` 的自过滤保留）。

## v0.3.2

QQ 号改为「萌萌小方块」展示。

增强点：

- 卡头的 QQ 号原来是白色文字直接印在粉色渐变上，对比度不足看不清。现改为每位数字一个**白色圆角小方块**（34px、圆角 9），深梅色数字居中，方块交替 ±4° 微倾制造萌感；「QQ」标签同步改为深梅色。数字多为 10 位左右，方块行宽约 440px，不会触边。

## v0.3.1

等级徽章排版重做：图标与数字分离，修正长昵称下的溢出。

增强点：

- 徽章字号 30 → 26；图标装进**纯白实底胶囊**（左右各 12px 对称留白，原来半透明磨砂底对比度不够且左右不对称），「57级」数字作为深色文字独立放在胶囊右侧，不再和图标挤在一起。
- 修正长昵称下的布局 bug：`_fit_title` 给徽章预留的宽度（measure+20）小于胶囊实际宽度（measure+48），昵称越长越接近截断上限时，胶囊会向右溢出预留区约 28px。现在徽章总宽（标题间距 + 胶囊 + 数字）由 `_level_metrics` 统一计算并精确预留；长昵称先缩字号再省略号截断，徽章始终完整落在卡内。

## v0.3.0

新增本地数据库记录入群/退群精确时间；等级徽章可读性优化。

新功能：

- 入群/退群精确时间记录（`record_join_leave` 配置，默认开启）：新增 `core/store.py`（SQLite 标准库实现，零新依赖），数据存于 `plugin_data/astrbot_plugin_box/member_times.db`。在监听到 `group_increase` / `group_decrease` 事件时写入精确到秒的本地时间；查询在群成员时顺带回填接口的 `join_time`（`INSERT OR IGNORE`，事件精确值优先不被覆盖）。卡片上的「加群时间」优先使用库内精确值（`YYYY-MM-DD HH:MM:SS`）；**退群成员接口查不到任何信息，现在靠库可以显示「加群时间」和「退群时间」两行**；重新入群会更新入群时间并清除退群时间。
- 「退群时间」注册为渲染层识别的正式标签，独立成行、与「加群时间」同样式。

增强点：

- 等级徽章加白色磨砂胶囊底板：金色皇冠/太阳/星星落在粉色渐变卡头上对比度不足，白底胶囊后清晰可读，风格不变；移除卡头黄星装饰避免与徽章重叠。

## v0.2.2

修复群等级「0级」；QQ 等级增加多字段名兼容与诊断日志。

Bug Fixes：

- 群等级偶现「0级」：部分适配器把 `get_group_member_info` 的 `level` 返回成字符串 `"0"`（truthy），旧逻辑 `if self.group_level` 判真后直接 `int()` 输出「0级」。现在先转 int、非正数直接不显示该行。
- QQ 等级取不到时完全静默，无从排查。现改为多字段名兼容：按 `qqLevel` → `qq_level` → `level` 依次尝试（NapCat/LLOneBot/Lagrange 字段名不一致），0/缺失/非法值一律视为无数据；仍取不到时输出 INFO 日志并附上接口实际返回的全部字段名，便于确认是否为适配器侧不提供。

增强点：

- 等级徽章数字改为「57级」格式直出（原来是小号括号 `(57)`），便于一眼确认数据链路是否打通。

## v0.2.1

修复热更新后新代码不生效的问题；AI 分析拆分为三个独立开关。

Bug Fixes：

- `main.py` 新增依赖模块强制重载列表（`__init__` 里按依赖顺序 `importlib.reload` core/ 全部子模块，重载后重新绑定 `PluginConfig` / `BoxService` 类）：AstrBot 热更新只重载 main.py，`core/draw.py` 等子模块残留在 `sys.modules` 里继续跑旧代码，导致「更新后卡片样式/行为不变」——v0.1.1 / v0.2.0 的新排版与等级徽章在热更新的实例上不可见即此根因。从旧版本升级后建议完整重启一次 AstrBot，清掉已被污染的模块缓存。
- 等级徽章改为结构化传递：`BoxResult.level_text` 由 service 直接从 profile 数据生成（含「等级隐藏」），并从字段行中移除「QQ等级」行，不再依赖渲染层的行解析。只要接口返回了 `qqLevel` 数据就一定显示在昵称旁；若开启后仍不显示，说明 NapCat 的 `get_stranger_info` 未返回 `qqLevel` 字段（数据缺失，非渲染问题）。

增强点：

- AI 分析拆分为三个独立开关（`ai_analysis.avatar_analysis / signature_analysis / overall_analysis`，替代 v0.2.0 的单一 `llm_analysis`）：头像印象 / 签名解读 / 综合锐评三项并发调用、卡片上分区展示；签名解读在对方没有签名时自动跳过；未配置 LLM Provider 或单项调用失败不影响其他项与正常出卡。

## v0.2.0

卡片二次重设计 + 三个新功能（AI 锐评 / 入群排位 / 获取时间）。

新功能：

- AI 锐评（`llm_analysis` 配置，默认关闭）：开启后用当前 LLM 提供商分析用户头像图片与个性签名，生成一句轻松幽默的点评，以「萌萌锐评 · AI」黄色便签块展示在卡头下方。Provider 为空或调用失败时静默跳过，不影响出卡。
- 入群排位：群聊场景下通过 `get_group_member_list` 按 `join_time` 排序，计算目标用户是本群第几位加入（「第 128 位 · 共 345 人」），作为首行展示；私聊或接口失败时不显示。
- 卡片右下角显示资料获取时间（`%Y-%m-%d %H:%M`）。因卡片含时间，渲染缓存随之移除，改为每次实时渲染（PIL 绘制放入 `asyncio.to_thread`，不阻塞事件循环）。

增强点：

- 字体更换：站酷庆科黄油体 → 思源圆体 CN Medium（Resource Han Rounded，SIL OFL，随包附 `OFL-License.txt`），字形更干净圆润；旧 `box.ttf` 移除。
- 排版重整（去花哨）：字段行改为统一浅灰粉底色、统一玫瑰粉标签色，移除四色轮换；「昵称」「QQ号」「签名」提升进卡头——签名以引号样式显示在 QQ 号下方（最多两行，超出省略），「QQ等级」带图标时以徽章形式紧跟昵称之后（`隐藏` 等无图标值仍留在字段行）。
- QQ 等级图标改为代码自绘矢量（皇冠/太阳/月亮/星星，金色系），不再依赖 emoji 字体是否收录该字形——解决「看不到等级图标」问题；生肖等其他 emoji 维持 cmap 检测 + 行标签色剪影渲染。

## v0.1.1

卡片渲染器重设计：与 logo 统一的粉彩萌系风格，卡片更大、信息更清晰。

增强点：

- 卡片整体重绘：粉色渐变卡头（大圆头像 + 昵称大标题 + QQ号副标题）、白色卡身、字段行淡彩圆角分区（粉/紫/蓝/黄轮换，与 logo 信息条同色系）、带猫咪图标的品牌页脚；画布加宽到 1040px，布局不再受文字高度限制。
- 「昵称」「QQ号」提升为卡头展示（缺失时依次回退「群昵称」「备注」），其余字段按配置顺序排列。
- 头像改为居中裁剪圆形 + 白色描边（原来直接压扁成文字高度）；拉取失败时使用内置猫咪占位头像（原来是一块 640px 白图）。
- 长文本自动换行（签名/地址/邮箱等），外部库多值行在同一分区内对齐；超长昵称先缩小字号、再以省略号截断，不再溢出卡片。
- emoji 渲染修复：随包 `NotoColorEmoji.ttf` 是裁剪过的单色轮廓字体（仅 343KB、无彩色位图表），原来 emoji 渲染成白色剪影或豆腐块。现按字体 cmap 检测字形：缺失的字符静默跳过，支持的 emoji 以所在行的标签色着色渲染，QQ等级图标（👑🌞🌙⭐）与生肖 emoji 随行配色、风格统一。`requirements.txt` 新增 `fontTools`（仅用于读取 cmap）。
- 移除旧版随机元素（随机边框色、每行随机文字颜色），配色固定，同数据渲染结果稳定，利于缓存复用。

Bug Fixes：

- 渲染缓存摘要加入渲染器版本号（`render: 2`），升级后旧格式的缓存卡片自动失效重绘，不会一直显示旧样式。

## v0.1.0

Fork 重构：基于 Zhalslar/astrbot_plugin_box v1.4.0 更名换装，功能逻辑保持不变。版本号自本 fork 起重新计数。

- 展示名更改为「萌萌资料卡」，重绘 logo，`metadata.yaml` 的作者与仓库信息改为 windExplorer（技术名 `astrbot_plugin_box` 保持不变，与目录名、仓库名一致）。
- 主指令由 `/盒` 改为 `/资料卡`，保留 `盒` / `开盒` / `box` 作为别名，旧用法不受影响。
- 配置页与文档字符串去除「开盒」表述，统一为「查询 / 展示资料卡」。
- README 重写：补充原项目致谢；修正许可证徽章（原 README 标注 GPLv3，实际 LICENSE 为 AGPL-3.0）。

## v1.4.0

新功能：

- 引入 `BoxService` 服务层，用于处理 QQ 资料查询、数据转换以及卡片图片渲染。
- 新增 `BoxUserProfile` 模型，用于规范化和格式化 QQ 用户数据，包括可选的外部库信息，并支持可选的脱敏处理。
- 在启用时，通过可配置的 `mystery_url` 和 `cookies` 支持基于外部库的资料扩展。

增强点：

- 重构 `BoxPlugin`，将数据与渲染逻辑委托给 `BoxService`，简化命令处理，并改进撤回任务管理。
- 通过 `CardTheme` 配置让卡片渲染支持主题化，并调整文本、边框及表情布局行为。
- 精简配置处理流程，增加插件选项并提供更清晰的日志信息，同时新增 `library_switch` 辅助方法。
- 通过使用 `At` 组件和原始消息文本改进提及解析，实现更健壮的目标 ID 提取。

## v1.3.1

New Features:

- 为 box 命令新增别名 `"box"`，以便更轻松地调用。

Bug Fixes:

- 通过集中、健壮的生日解析逻辑修复生日、星座和黄道十二宫字段的计算，并优雅地处理无效日期。

Enhancements:

- 修改库客户端的初始化方式以使用其默认配置。
- 将用户信息查询默认设置为发送图片，除非显式禁用。
- 将生成的 box 卡片图片存储在插件专用的临时目录中，而不是持久化缓存目录，并移除在终止时清理缓存的逻辑。

## v1.3.0

为查询和渲染 QQ 个人资料卡片新增可由 LLM 调用的工具，并将 box 工作流重构为更清晰的数据、发送和召回层。

New Features:

- 暴露一个供 LLM 使用的工具入口，用于查询 QQ 用户资料信息，并可选支持图片生成。
- 引入 `BoxResult` 容器，用于统一 box 查询结果，支持文本和图片两种响应形式。

Enhancements:

- 将 box 命令流程重构为独立的数据获取、渲染、发送和召回职责，并提供更清晰的错误处理和配置检查。
- 优化自动入群/退群处理逻辑，使其使用新的核心 box 信息管线以及事件拦截/停止机制。
- 简化并增强生日、星座、生肖和地址等计算字段的生成逻辑。
- 调整渲染图片的缓存键格式，使其与群 ID 解耦，并简化缓存清理与日志记录。
